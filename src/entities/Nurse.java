package entities;

import enums.Sex;

public class Nurse extends Person {
    public Nurse(String firstName, String secondName, int age, Sex sex) {
        super(firstName, secondName, age, sex);
    }
}
